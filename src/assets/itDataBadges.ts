import badgeStarter from './cloud_basico.png'
import badgeBuilder from './cloud_intermedio.png'
import badgeExpert from './cloud_experto.png'

/** Insignias IT Data (Starter → Builder → Expert) */
export const IT_DATA_BADGE_IMAGES = {
  starter: badgeStarter,
  builder: badgeBuilder,
  expert: badgeExpert,
} as const
